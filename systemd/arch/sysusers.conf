# Creates the midi-daemon system user and group.
# Install to /usr/lib/sysusers.d/midi-daemon.conf
# The service uses SupplementaryGroups=audio for ALSA access,
# so no explicit audio group membership is needed here.
u midi-daemon - "MIDI Daemon" - -
